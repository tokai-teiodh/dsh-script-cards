# dsh-script-cards 变更记录

版本号写在两个地方：`package.json` 的 `version` / `dsh.versionTag`，以及 `lib/client.js`
里的 `VERSION`（面板页眉右上角那个角标，由 `dsh.versionTag` 生成）。git tag 与面板角标
同名，便于一一对应。公开仓库从 `v0.1.0-alpha.1` 开始发布，更早的 `v-alpha-0.1` /
`v-alpha-0.2` 只有本地历史。

源码在 `src/`，`lib/client.js` 由 `node scripts/build.mjs` 拼装生成，不要直接改产物。

## v0.1.1

第二个正式版，代码和 v0.1.1-alpha.3 一样，只把版本号里的 alpha 去掉了。那一版他在真机上试过。

从 v0.1.0 到现在，一共这些：

- 自动排列不再把卡片自己推下去：卡片的位置只由卡片决定，右边那列选项溢出来的部分只算间距。
- 连线平时什么都不显示，点一下才浮出输入框；已经起过名字的线一直挂着名字，点名字就能改。
- 浮层宽度统一成 460，输入框都带 `box-sizing:border-box`，对话框不再多出一条横向滚动条。
- 标签条的原生滚动条不动时完全看不见，按住标签条就能横拖。
- 点开的卡片里，滚轮只滚那一页，画布不跟着缩放。
- 画布上右键拖出虚线框可以选多张，整组拖动、批量染色 / 移出画布 / 删除；`Delete` 整组移出，`Ctrl+A` 全选这一层。
- 四种卡片改成竖版，更适合竖着的侧栏。
- 方片页不再能拖选文字，顺带修掉拖选松手那一下会把面板整块搞崩的毛病。

两套无头测试现在是 171 + 316 条；`tests/visual.mjs` 把真样式表丢进无头浏览器截图比对。

## v0.1.1-alpha.3

都是他试完 alpha.2 之后提的。这一版是 v0.1.1 正式版之前的候选版。

### 卡片改成竖版

以前的卡片是 2.3:1 的横条（节点 184×80、章节 208×96），流程往右一层层排出去就是一条又长
又扁的带子 —— 侧栏是竖的，怎么看都不合身。现在四种卡片都是竖着的：章节 176×120、
节点 156×112、条件 / 结果 144×86（高约等于宽的 0.6～0.78）。挂在这张卡右边的选项列
跟着收窄到 156（比卡片还宽，看着就像另一张卡了）。

卡片高了之后内容也跟着摆：简介从「紧贴标签」改成钉在卡片下沿（跟方片页一致），
条件 / 结果卡那两行字竖直居中。

顺带修了一个小数像素的老毛病：展开卡片、滚轮缩放时写进 state 的 x/y 是小数，而画的时候
要 `round` 到整像素 —— state 和屏幕上真正的样子最多差半个像素，命中测试和连线起笔点都吃
这个亏。现在这两处也取整了。

### 点开卡片之后，滚轮归卡片

展开一张卡片就是打开了一页内容，可滚轮还在缩放画布 —— 想往下看正文，画布先放大了一格。
现在指针落在展开的卡片里，滚轮就只滚它（也不去拦默认行为），画布不动；指针在卡片外面，
滚轮照旧缩放。

### 画布上可以右键拖框选多张

右键拖出一个虚线框，框到的卡片一起选中；按住其中任何一张拖，整组跟着走（每张的相对位置
不变）。框完之后在选中的卡片上点右键，是一个批量菜单：移出画布、染色、删除这 N 张卡片
文件，还带「选中这一层全部」和「取消选择」。键盘上 `Delete` 整组移出画布、`Ctrl+A`
全选这一层。

右键**没有**拖动的那一下仍然是原来的菜单（卡片的、空白处的），框选只在画布上生效；
整组移动只写一次图谱文件（逐张写会互相覆盖）。

两套无头测试现在是 171 + 316 条，测试里的卡片尺寸都改成从渲染结果里读（不再写死 184/80），
以后再调尺寸不会连带弄坏一堆断言。新增的断言照样反着验过：把滚轮那条判断去掉，
AE 那两行立刻变红。

## v0.1.1-alpha.2

alpha.1 之后他在真机上又试了一轮，回来三条：一条崩溃、两条手感。

### 在方片页拖动，面板会整个崩掉

拖选文字本来没什么，坏在松手时那一下点击：它打开了右边那张卡片的详情，而详情一渲染，
面板就抛 `Rendered more hooks than during the previous render`，然后整块白掉，点什么都没反应。

根子是有些组件被当成普通函数直接调用了（`Tile({...})` 这种写法）。那样写，被调用组件的
hook 会算到**调用者**头上；调用点的数量一变（选中卡片、格子多少变了），hook 数量跟着变，
真 React 就报错。现在这类调用一律走 `createElement`，各是各的组件。顺手把方片页设成
不可拖选（正文在右边的详情里，那里照样能选）。

同一个替身测试也补上了这条判据：hook 顺序或数量一变就报错，跟真 React 一样。
以前这类崩溃在无头测试里完全看不见，所以它一路全绿。

### 标签条的滚动条藏掉之后，整条也就滑不动了

原生滑块没了，鼠标就没有可抓的地方。现在按住标签条左右拖就是滑动，拖动时那根细条照样
亮出来，拖完那一下点击不会再顺手打开卡片。拖动时标签文字也不会被选中。

### 点连线浮出来的输入框，被卡片压掉了一半

线的中点常常落在某张卡片底下，而标签的 DOM 顺序在卡片前面，于是只露半个。现在标签和
输入框有自己的层级（6 / 7），压得住卡片和旁边的选项列，又还在展开的大卡片下面。

两套无头测试现在是 171 + 292 条。这一轮新加的断言照样反着验过：把组件改回普通函数调用、
把 `z-index` 删掉、把 `pointerdown` 摘掉，对应的断言都会红；无头浏览器的探测里专门摆了
一张压住连线中点的卡片，`z-index` 一删，`elementFromPoint` 立刻量出标签被埋了。

## v0.1.1-alpha.1

上一版（v0.1.1）还没发出去，他先在真机上试了，回来四条，全是这版修的。版本号按他的意思
改成预发布 —— v0.1.1 从来没有发布过，没有 tag 被挪动。

### 自动排列还是不对，卡片被自己的选项列推下去了

上一版让自动排列按「卡片 + 右侧选项列」整块让位，可是顺手把选项列的高度也算进了卡片自己
的位置里，卡片在那块地方里居中 —— 结果一层里最上头那张分歧卡片会比旁边的卡片低一截。
现在卡片的位置只由卡片自己决定，选项列溢出的那部分只体现在两张卡片之间的间距上。

### 连线平时不该显示东西

上一版给每条线都挂了一个淡的「连线」当提示，太吵。现在线上平时什么都不显示，**点一下
那条线**才浮出输入框，回车或点别处保存、Esc 放弃；已经起过名字的线，名字一直挂在线上，
点名字就能改。右键那条线还是菜单（重命名 / 删除）。起名字不再弹对话框。

### 输入框比展开的卡片宽，下面还多一条滑条

输入框是 `width:100%` 又没写 `box-sizing:border-box`，左右内边距加描边让它比容器宽出
18px，对话框里就多出一条横向滚动条。现在所有对话框都用 `border-box`，宽度统一成 460
（和展开的卡片一样）—— 以前是 380 / 420 / 520 各一档，点来点去界面宽度一直在变。

### 主页卡片的滑条

标签条的滚动条改成「不动的时候一点都看不见，滑的时候才浮出一根细条」：原生那条整个藏掉
（Windows 会给它配两侧的三角箭头，很丑，也不占位了），自己画的细条是绝对定位的覆盖层，
出现和消失都不挪动任何布局。

「＋ 选项」按钮的高度在 CSS 里定成 30px，自动排列按它算占位，两边对得上。

两套无头测试现在是 171 + 277 条。这一轮新加的断言都反着验过：把选项列的高度算回卡片的
位置里、或者把标签塞回 SVG 里，对应的断言都会红。

## v0.1.0

第一个正式版。代码和 v0.1.0-alpha.6 一样，只把版本号里的 alpha 去掉了。那一版他在真机上
试过：拖卡片的时候线跟着走，圆点是细圈，开着面板照样能在对话里粘贴。

到现在它能干这些事：方片网格看全貌，两级分支画布理章节和情节，卡片能拖、能连、能染色、
能展开，改完写回文件 —— 内容在 `卡片/*.md`，结构在 `分支.json`，两样都能进 git。

怎么知道它没坏：宿主侧和浏览器侧各一套无头测试，171 + 233 条；另外 `tests/visual.mjs`
会把真样式表丢进无头浏览器截张图。这几轮修的三个「看不见」的毛病，都是它先看出来的 ——
选项列被裁掉、连线画到屏幕外 4000 像素、圆点太粗看着不像圆。那个 React 替身没有排版
引擎、也不跑 CSS，所以这类问题在断言里全是绿的。

### 一路修过来的
- 落盘桥（`lib/index.js` + `lib/typert.js`）：浏览器端通过 Typert 拿到 `scriptCardsFs`，
  六个文件方法都以 `root` 为边界，桥挂了面板就退成只读，并把原始报错写在顶上。
- 目录名可以改（默认 `剧本档案/卡片/归档`），色卡可以自己加（只存在这台浏览器）。
- 画布：滚轮缩放、空白处左键平移、拉圆点连线、自动排列、染色、展开动画、后台式堆叠条；
  快捷键只在画布上生效，不抢对话输入框的粘贴。
- 逐版修的东西见下面 alpha.1 ~ alpha.6。

## v0.1.0-alpha.6

作者在真机上试过，确认没问题，随后作为 `v0.1.0` 正式发出去。

### 拖卡片的时候，连线不跟着走
卡片是按「手在哪」画的，连线取的是文件里存的坐标。同一帧里两套数，于是卡片跟着手走、
线钉在原地，松手才跳过去。现在两边都走 `liveRect(k)`，拖着的时候线就跟着走。
没有额外开销：拖动本来每帧就要重画一次。

### 圆点还是不像圆
上一版把圆点做成 16px、偏移取整、选项行固定 30px，解决了圆心落在半像素上的问题。
但把画布按真机缩放（96 DPI，也就是 1 倍）渲染出来、再放大十倍看：2px 的圈套在 16px
的点上，栅格化出来是个厚八边形，当然不像圆。圈改成 1px、平时透明度提到 .9，就对了。

### 顺手
`tests/visual.mjs` 加了 `--dpr`，可以按真机缩放渲染；`--probe` 会另写一份
`.visual/probe.json`，方便拿坐标去放大看某个元素的真实像素。

## v0.1.0-alpha.5 — 连线看得见了 · 选项居中 · 快捷键不再抢对话输入框

面板角标 `v0.1.0-alpha.5`，包版本 `0.1.0-alpha.5`。

### 修复：连线「没有渲染出来，是全透明的」
- 症状：卡片之间的连线一条都看不见（连线标签「连线」倒是画出来了）。
- 两个根因叠在一起，缺一不可：
  1. **整层线画在屏幕外 4000px 处**。连线层 `.sc-edges` 在 CSS 里被挪到 `-4000,-4000`
     （为了让坐标为负的卡片也能画），但 SVG 自己的用户坐标系跟着挪了，路径坐标是**画布坐标**，
     没有任何一处补回这 4000 —— 于是每条线的实际位置都是「画布坐标 − 4000」。
     连线标签是普通 DOM、直接按画布坐标摆的，所以标签在、线不在。
     修法：画路径的那个 `<g>` 加 `transform="translate(4000,4000)"`（常量 `EDGE_PAD`，
     CSS 里的 `left/top` 与它必须一致，测试盯着这一点）。
  2. **线的颜色等于透明**。`stroke:var(--dsw-alias-border-l2)` 是「描边级」的颜色：
     深色主题下算出来是 `rgba(255,255,255,.12)`，浅色下是 10% 黑。在浏览器里量出来的
     就是这个值 —— 一条 1.5px、12% 白的线，在画布上等于没有。改用次要文字色
     `--dsw-alias-label-secondary`（深色 `#cfd3d6` / 浅色 `#61666b`，两套主题都在）。
- 顺带把线宽**统一**：普通 / 高亮 / 橡皮筋全是 `1.5px`，只差颜色与透明度；
  箭头改 `markerUnits:userSpaceOnUse`，固定 8×8（以前跟着 `stroke-width` 放大，
  同一个箭头在普通线和高亮线上是两个尺寸）。

### 修复：接口圆点「不是正圆」
- 圆点是 `border-radius:50%` 的方盒子，所以**尺寸或位置落在半像素上就不圆**。
  选项行以前是 padding 撑出来的（约 28.67px 高），圆点跟着落在 `.33px` 上，
  栅格化出来是一团发虚的椭圆。
- 修法：圆点 14px → **16px**、偏移 ±8px（`top:50%` 配 `margin-top:-8px`）；
  选项行高**固定 30px**（单行、超长省略号 + `title`，展开卡片里读全文），
  于是 15−8=7 都是整数。取消 `.sc-port.hot` 上的 `scale(1.3)`（缩放同样会让 16px 的小圆发虚），
  改成「被当靶子时整颗填实」。默认透明度 .45 → .65。

### 修复：分歧节点右侧的选项改成上下居中
- 选项列原来固定挂在卡片 `top:8px`。现在整列按**选项本身**的高度居中；
  下面的「＋ 选项」挂在最后一行下面、**不参与居中**（用户的要求）。
- 没有布局引擎可测量，所以列顶由 JS 算出来写成行内 `top`（`choiceTop()`），
  连线起点用的是**同一套几何**，两边不会各说各话；取整，落在整数像素上。

### 修复：连接逻辑
- **一个选项只能有一条出线**：把某个选项改连到别的卡片时，旧的那条先摘掉。
  以前是「再加一条」，同一个选项会拖着两条线、分别指向两个节点。
- **两个选项指向同一张卡，两条线都留得住**。图谱去重原来只按 `from+to`，
  第二次读盘会把后一条当成重复的扔掉 —— 卡片上写着已连接、线上却没有线。
  用户自己那份 `分支.json` 里正是这样：选项A 与 选项B 都写着指向「结果」，
  `edges` 里却只剩一条。去重键改成 `from+to+choice`。
- 橡皮筋的起点改成**量**出来的：直接读那颗圆点的 `getBoundingClientRect()` 中心，
  选项行高、卡片展开后的高度怎么变都对得上（量不到才退回按几何算）。
- 拉线时「哪里能落」现在看得见：其它卡片的**入口**点亮成靶子，源卡片自己不亮；
  端口只认左键（右键在端口上仍旧弹菜单）。

### 修复：打开面板后对话输入框粘不进字
- 症状：打开剧本档案侧边栏，主界面对话框里 `Ctrl+V` 粘贴失效。
- 根因：本面板在 `window` 上挂着全局 `keydown` 快捷键（`Ctrl+C/X/V/D`…），
  只在 `input / textarea` 上让路。而 DSH 的对话输入框是 **contenteditable**（Lexical），
  两个标签都不是 —— 于是 `Ctrl+V` 被面板 `preventDefault()` 吃掉了。
- 修法：可编辑区判断扩展到 `contenteditable`（连同 `isContentEditable`），
  并且快捷键**只在「最近一次点击落在画布上」时**生效（捕获阶段记一笔，
  卡片/选项自己 `stopPropagation` 也拦不住）。点过对话输入框之后，快捷键就还给宿主。

### 顺带：标签条悬停不再改尺寸
- 标签条原来是 `::-webkit-scrollbar{height:0}` + `:hover{height:5px}` —— 悬停会**改变元素
  高度**：滑块一出现就把标签顶走，指针落回原处又取消悬停，缩回去又悬停，于是高频来回抖，
  整块网格跟着跳。悬停只该改颜色：现在这 5px 位置一直留着，只有滑块的颜色在变。
- 这只是「侧栏里可能出现的抖动」的一种。主界面那块如果真的在跳，更可能是别的插件
  （见下面那条说明），这条只是把本项目里唯一一处「悬停改尺寸」清掉。

### 新增：`tests/visual.mjs`（可选的视觉回归）
- 把**真实**的 `src/10-css.js` 喂给无头 Edge/Chrome，渲染一张分支画布截图
  （`npm run visual`，`--theme light`、`--zoom 0.5`、`--probe` 打印计算样式）。
- 主题色优先从装好的 DSH 的 `app.asar` 里读真值，读不到用内置兜底值；
  它还会顺手断言「每条线的 stroke 不是 none」「每个圆点都有描边」。
- 为什么留这个工具：无头测试的 React 替身**没有布局引擎、不跑 CSS**，
  上面那两个「看不见」的 bug（画到屏幕外、被裁掉）在断言里全是绿的。
  这一课踩过两次了。

> 关于「主界面对话框莫名上下跳动」：这个面板自己的 DOM 全部在 `.sc-*` 里，没有一条
> 全局选择器、没有一个定时器（唯一的 1 秒定时器在别的插件里），所以查过之后没有发现
> 它能推动主界面布局的地方 —— 上面那条标签条悬停是唯一一处「会改变自己尺寸」的写法，
> 已经清掉。其余嫌疑在本项目之外（本机还装了折叠/主题一类的插件，它们直接改对话区
> 的 DOM 与样式）。判断办法见提交说明。

## v0.1.0-alpha.4 — 选项列回来了 · 滚轮缩放 · 左键拖画布

面板角标 `v0.1.0-alpha.4`，包版本 `0.1.0-alpha.4`。

### 修复：分歧节点右侧「还是什么都没有」（选项列被卡片自己裁掉了）
- 症状：分歧卡片右边看不到选项，接口圆点也像是残缺的半个；可展开卡片时选项明明列得出来。
- 根因：`.sc-card` 上有 `overflow:hidden`。选项列是**贴在卡片盒子外面**的
  （`left:100%; margin-left:14px`），卡片一裁溢出，整列选项连同每一项自己的出口圆点被裁得
  干干净净；`left:-7px` / `right:-7px` 的接口圆点也被裁成了半个 —— 就是「所有卡片的节点
  都没用了」时看到的样子。
- 修法：卡片改成 `overflow:visible`（卡片内部该滚的地方本来就各自带 `overflow:auto`），
  选项列加 `z-index:2`，免得被旁边的卡片盖住。
- 无头渲染器没有布局，看不到「被裁掉」，所以这条用**样式契约**钉住：`.sc-card` 里不许
  出现 `overflow:hidden`（T 段）。

### 接口圆点：整个圆 + 空心
- 从 16px 实心（本色填充 + 底色描边）改成 14px **空心**：底色做底、本色一圈 2px 描边，
  左右各探出卡片 7px，是整个圆而不是半个。
- 平时就带一半透明度，悬停 / 选中 / 拉到一半时整亮 —— 以前默认 `opacity:0`，
  不把鼠标悬上去根本看不出哪里能拉线。

### 画布操作改成通用习惯
- **滚轮 = 缩放**（以指针为中心），不必再按 `Ctrl`。滚轮让给缩放之后原来的滚轮平移就没了，
  所以留两条路：`Shift+滚轮` 左右移、`Alt+滚轮` 上下移；`Ctrl/⌘+滚轮` 照样缩放
  （触控板捏合走的就是它）。
- **左键在空白处拖动 = 平移画布**（以前只有中键 / `Alt+左键`）。卡片上的按下不算：卡片自己的
  拖拽会冒泡到画布，那一路必须让开，否则一次拖动会被两条路同时接管。平移时画布光标变抓取手势。
- 滚轮监听改成用 `ref` 自己挂、`{passive:false}`：React 是把 `onWheel` 注册成 passive 监听器的，
  里面调 `preventDefault()` 无效 —— 于是滚轮一边缩放、页面一边跟着滚（或跟着缩放）。
- 拉线时的橡皮筋从**你拉的那一项自己的出口**起笔。以前一律从卡片右侧正中起笔，
  分歧节点上选项有好几个，起点是歪的。

### 测试
- `tests/smoke.mjs` 182 条（新增 22 条）：J（默认滚轮缩放）、R（空白处左键平移，以及卡片上的
  按下不被接管）、S（`Shift`/`Alt`+滚轮平移）、T（选项列不被裁、端口是空心圆、平时可见）、
  U（橡皮筋起点在选项上）。每条都验证过「把旧写法放回去会变红」。
- `tests/mini-react.mjs` 补了元素级监听器（按渲染路径存），测试才能派发插件用
  `ref + addEventListener` 挂上去的 `wheel`。
- `tests/host.mjs` 171 条不变。合计 353 条。

## v0.1.0-alpha.3 — 拖得动了 · 分歧节点能用了 · 画布不糊了

面板角标 `v0.1.0-alpha.3`，包版本 `0.1.0-alpha.3`。

### 修复：拖动卡片不写回文件（看起来就是拖了没反应）
- 症状：画布上把卡片拖到新位置松手，位置没落住（跳回原处），`分支.json` 里还是旧坐标。
- 根因：松手时读的是 **React state** 里的坐标。真实浏览器里 `pointerup` 紧跟在
  `pointermove` 后面派发，而此时 `setState` 还排在一个宏任务里没重渲染 ——
  闭包读到的还是上一帧、甚至是按下时原位的值。无头测试里 `setState` 是同步重渲染的，
  所以这个洞一直没暴露。
- 修法：拖动中的实时坐标只放 ref（`dragRef.current.x/y`），`pointerup` 只认 ref。
- 测试：给无头渲染器加了 `defer(true)` / `flush()`，能造出「state 还没重渲染」这个时间差。
  N 段在 `pointermove` 与 `pointerup` 之间**不让它渲染**，直接断言落盘坐标。把旧写法
  放回去，这条断言立刻变红。

### 分歧节点：选项真的连到节点上，而且可以直接新建
- 以前从选项出口拉出的连线只记进图谱的 `edges`，**没有写回选项自己**，于是展开卡片永远
  显示「还没连到节点」；选项列表也只能从右键菜单或展开面板里改。
- 现在：连线 / 断线会同步维护 `choices[].to`（拖接通就记下目标，右键删线就清空）；
  卡片上的选项列多了「＋ 选项」按钮（`data-choice` 那个出口仍在，可拖），
  接通后标出走向（`→` 与 `linked` 样式），展开面板直接写「选项甲 → 节点一」。
- 以前「分歧节点」只能先建普通节点再右键改形态，且新建时 `mode` 没写进图谱 ——
  文件里是分歧、画布上却没有选项列。现在空白处右键有**新建分歧节点**，编辑器预置形态为
  「分歧」，保存时 `mode` 同时写进卡片文件与图谱。
- 选项列的渲染也认卡片文件里的 `mode`：`cards.py` 手写的 `mode: branch` 卡片不必等图谱补记。

### 章节卡片不再有「时间」
- 章节是容器，故事内时间是章节里那些节点的事。编辑器对章节不再显示时间输入框；保存时顺手
  清掉卡片文件里残留的 `when` 行；卡片与底部堆叠条上也不再印时间。

### 画布不再糊
- 症状：画布（尤其缩放 / 平移过之后）整块发虚，文字边缘发毛。
- 三处原因一起修：
  1. `.sc-stage` 上的 `will-change:transform` 把这层（里面有 8000×8000 的点阵与 SVG）
     钉成一个合成层，缩放 / 平移时浏览器拿旧位图拉伸 —— 删掉。
  2. 只有「展开时把卡片飞到中央」需要过渡；平移 / 缩放也套 `.28s transition`，既跟不上手，
     又让浏览器一直拿旧位图做动画 —— 现在只有展开那一下有过渡。
  3. 层的 `translate` 取整到整像素（小数偏移会让整层文字失去像素对齐）。
- 缩放改成**档位制**（25 / 33 / 50 / 67 / 80 / 100 / 125 / 150 / 200%）：`Ctrl+滚轮` 与
  `＋` `－` 都走这些档。自由缩放（1.12 的幂）会让整块画布按奇怪比例重新栅格化，越缩越糊。
- 顺带：滚轮的 `deltaX / deltaY` 缺失时按 0 处理，不再让 `NaN` 混进视口把整块画布废掉。

### 测试
- `tests/smoke.mjs` 160 条（新增 40 条）：N（拖动与重渲染之间的时间差）、O（选项与节点绑定 /
  断线 / ＋选项）、O2（直接新建分歧节点走完整条路）、P（章节没有时间）、Q（整像素、无过渡、
  无 `will-change`、缩放档位）。每条新断言都验证过「把旧写法放回去会变红」。
- `tests/mini-react.mjs` 补了两个能力：`defer / flush`（`setState` 不再同步渲染），以及
  **卸载时清掉 hook state 与 effect** —— 以前关掉对话框再打开会带着上一次的输入，
  因此漏测过「新建分歧节点应当预置形态」这类东西。
- `tests/host.mjs` 171 条不变。合计 331 条。

## v0.1.0-alpha.2 — 右键菜单点得动了 · 新建真的会上画布

面板角标 `v0.1.0-alpha.2`，包版本 `0.1.0-alpha.2`。

### 修复：右键菜单里每一项都没反应
- 症状：在画布空白处右键 → 新建（复制 / 粘贴 / 染色 / 展开 / 自动排列同样），点下去
  什么都不发生。用户报的是「右键新建弹不出来东西」。
- 根因：关菜单用的是 `window` 上**捕获阶段**的 `mousedown`。真实浏览器里点一项菜单的顺序
  是 mousedown → mouseup → click，而捕获阶段的 mousedown 在 click 之前就把菜单卸载了，
  click 于是落在空气上，菜单项的 `onClick` 永远不触发。
- 修法：菜单背后加一层透明遮罩（`.sc-menuback`），按下它就关菜单；`useDismiss` 只保留 Esc。
  菜单项自己的 click 不再被抢走。
- 为什么以前没测出来：无头测试是直接触发 `onClick` 的，从不模拟 mousedown → click 这个
  顺序。现在补了这一段，并且**验证过**：把旧写法放回去，那条断言立刻变红。

### 修复：新建 / 原地复制 / 粘贴出来的卡片不会出现在画布上
- 症状：卡片文件确实写进了 `卡片/`，但画布上什么都没有，等于「新建功能没有用」。
- 根因：`saveGraph()` 里会跑 `pruneGraph`，它只承认当前这一帧 `cards` 里的卡片；而刚创建的
  卡片要等下一次 `setCards` 才进 `cards`，于是**刚写进图谱的节点被当成孤儿当场删掉**。
- 修法：`saveGraph(next, keep)` 多接一个「这些键别当孤儿」的参数；新建、原地复制、
  粘贴到画布三处都把自己刚建出来的键传进去。

### 测试
- `tests/smoke.mjs` 120 条（新增 13 条）：M 段按真实事件顺序走一遍右键菜单，并完整跑完
  「新建节点 → 填标题 → 保存 → 落盘 → 上画布 → 挂在当前章节下」，粘贴副本也断言了画布节点。
- `tests/host.mjs` 171 条不变。合计 291 条。

## v0.1.0-alpha.1 — 落盘桥 + 两级分支画布

面板角标 `v0.1.0-alpha.1`，包版本 `0.1.0-alpha.1`。

### 新增：宿主半边落盘桥（这一版最大的变化）
- 浏览器端原来只有只读的 `remote.workspaceFiles`，改不动磁盘，所以画布结构只能
  塞在 localStorage 里，跟项目走不了。这一版给面板配了自己的**宿主半边**：
  - `lib/index.js` 注册 Cordis 服务 `scriptCardsFs`（`ctx.provide`），提供
    `list / readText / writeText / mkdir / remove / stat` 六个文件原语；
  - `lib/typert.js` 导出 Typert 清单（`exports["./typert"]`），客户端半边把自己的
    同名 descriptors 用 `ctx.remote.$mount(...)` 挂上，之后经
    `ctx.get('remote.scriptCardsFs')` 调用。
- **安全边界**：每个方法第一个参数 `root` 就是边界，所有读写都必须落在它之内
  （`..` 逃逸、相对路径、磁盘根目录都会被拒）。宿主因此不需要知道档案目录叫什么
  名字，目录名完全由面板决定。
- 桥不可用时（比如 Typert 网关没起来）面板自动退化成**只读模式**，顶部红条说明，
  画布退回浏览器 localStorage。
- 写文件走 `node:fs`：不经过 DSH 的沙箱层，换来的是不依赖会话 id / 沙箱策略解析，
  少一半失败面。越界由 `root` 边界兜住。

### 新增：结构图谱落盘
- 结构（章节归属、画布坐标、选项与连线、染色）存到
  `<工作区>/剧本档案/分支.json`，随项目走、可以进 git。
- 内容（标题 / 章节序号 / 时间 / 简介 / 标签 / 正文）仍然在卡片文件里。
- localStorage 只剩「这台机器上的偏好」：星标、置顶、上次的视图、缩放。
- 卡片文件被删掉后，图谱里对应的记录与连线在下次扫描时清掉并落盘。

### 变化：删掉世界线视图
- 世界线页整页删除（原话：没有什么用）。面板只剩两个视图：**方片** 与 **分支**。
- 在画布上改时间不再是「只改本机图谱」——直接写回卡片文件的 frontmatter，
  所以时间只有一份来源。

### 新增：两族卡片互不串门
- **存档族** `project / character / setting / scene / beat / dialogue / open / decision / reference`
  只出现在「方片」页。
- **分支族** `chapter / node / condition / result` 只出现在「分支」页。
- 已有的场次 / 节拍 / 台词素材不再进画布。

### 新增：两级画布 + 浏览器式导航栏
- 上级菜单排**章节**，下级菜单排该章节内部的**情节**。
  **双击章节卡片**进入下级；导航栏有后退 / 前进 / 回到上级 / 刷新 + 面包屑地址栏
  （`剧本档案 / G1.1 章节名`），行为照浏览器来。
- 底部仍是 iOS 后台式的卡片堆叠条：本级还没上画的卡片摆在里面，
  拖到画布上（落点对准指针）或点一下自动落位。

### 新增：四种卡片的形态
- **章节卡片**：章节序号 + 章节名分两段显示（可分别改），下面标签（单行横滑，
  滚动条不悬停不出现），最下面是简介。展开时下方多出该章节的节点列表。
- **节点卡片**：左上节点名、右上时间、下方简介；展开后简介让位，
  依次排列「角色 / 场景 / 内容」（从正文的 `## 角色` `## 场景` `## 内容` 小节读）。
- **分歧节点**：在节点基础上，右侧多出一列**选项**（贴在卡片右边），
  每个选项各自带一个可拖的出口，用来连到别的节点；选项数量可自由增删。
- **条件 / 结果卡片**：只有两行（类型 + 内容）；条件卡只有右侧出口，
  结果卡只有左侧入口；右键可以在条件与结果之间切换。

### 新增：右键菜单与快捷键
- 卡片上右键：复制 / 剪切 / 粘贴到此处 / 原地复制一张 / 染色 / 展开 / 移出画布 /
  删除卡片文件 / 改为条件或结果 / 改为分歧节点 / 编辑选项列表。
- 空白处右键：新建（章节或节点 / 条件 / 结果）、设置**常用类型**（新建时排最前）、
  粘贴、自动排列、清空画布位置、缩放归位——操作逻辑照 Windows。
- 快捷键：`Ctrl+C` / `Ctrl+X` / `Ctrl+V` / `Ctrl+D`（原地复制）/ `空格`（展开收起）/
  `Delete`（移出画布）/ `Ctrl+0`（缩放归位）/ `Ctrl+1`（100%）/ `Esc`（收起）。
- **染色**带一套 8 色预置色卡加原生色盘；预置色卡可以在「编辑色卡…」里换成自己的
  配色（自定义色卡只存这台浏览器，不会进代码仓库）。

### 新增：画布缩放与平移
- `Ctrl+滚轮`缩放（以指针为锚点），普通滚轮平移，`Alt`/中键拖动平移。
- 状态栏显示当前缩放比例，另有 `＋ / － / 归位` 按钮。

### 新增：展开动画
- 展开分两拍：先把视角平移到卡片居中（整块画布一个 transform，
  其它卡片的相对位置不变）→ 卡片放大 → 背景（含其他卡片）虚化 + 遮罩。

### 修复：首版整体只读（真机验证时才暴露）
- 症状：面板能开，但页眉一直显示「只读」，顶部红条说落盘桥不可用。
- 根因：Typert 网关挂载客户端清单前会跑 `assertMethodAvailable`，它拒绝
  **任何与 `RemoteNamespaceService` 原型或保留字段同名的办法**
  （保留字段：`ctx / empty / invokeRemote / methods / name / namespace`）。
  我原来把「删除文件」的方法叫 `remove`，而 `remove` 正好在原型上（它自己有个
  `remove(kind, method, token)`），于是 `remote.$mount(...)` 直接抛
  「client api: method "scriptCardsFs/remove" conflicts with its namespace service」，
  命名空间没装成，`ctx.get('remote.scriptCardsFs')` 永远是 undefined。
- 修法：六个方法统一改成 `fs` 前缀（`fsList / fsRead / fsWrite / fsMkdir /
  fsRemove / fsStat`），宿主半边、Typert 清单、客户端 descriptors、测试一起改。
- 顺手加了三道防线，免得同类问题再靠「重启才知道」：
  1. `tests/host.mjs` 断言每个方法名都不在保留名集合里；
  2. 客户端 `apply` 里做同样的自我检查，撞名直接在控制台报错；
  3. `$mount` 失败会重试 3 次，并把**原始错误文本**写进面板顶部那条提示。
- 另外补了一个竞态：桥挂好（或挂失败）时通知面板重渲染，不用手动点刷新。

### 新增：目录名可配置 · 色卡可自定义
- 面板右上角「设置」可以改三个目录名（档案总目录 / 卡片子目录 / 归档子目录），
  按项目存在浏览器本地。默认是 `剧本档案 / 卡片 / 归档`，和 `cards.py` 一致。
  落盘桥的边界由「档案总目录」决定，所以换名字不影响安全性。
- 右键菜单的染色块里多了「编辑色卡…」：可以增删预置色卡（最多 16 个），
  恢复默认就回到内置的 8 个通用色。自定义色卡只存在这台浏览器上。

### 其它
- **分歧节点展开后，选项在下面单列一块**：每条选项写出它的目标节点（没连就是「还没连到节点」），
  块尾一个「＋ 添加 / 编辑选项」快捷入口。
- 状态栏多了「自动排列」按钮（上级按章节号、下级按连线分层再按故事内时间），
  不用再去空白处右键里找。
- 简介由助手在扫描后批量生成并写回卡片 frontmatter：`cards.py summaries --missing-only`
  列出缺简介的卡片，生成后用 `cards.py set-summary` 只改 `summary` / `updated` 两行
  写回（正文一个字节不动）。面板会把缺简介的卡片数点出来。
- 测试分两半，都在 `npm run check` 里：
  - `tests/host.mjs`（171 条断言）——**宿主半边**：六个文件原语在真临时目录上跑通、
    UTF-8 中文往返、路径白名单六种越界全被拒、`typertRemote` 绑定形状、
    清单形状，以及**浏览器半边挂上去的 descriptors 与宿主清单逐端点对齐**。
  - `tests/smoke.mjs`（107 条断言）——**客户端半边**：两族卡片、两级画布、导航栏、
    四种卡片形态、右键菜单、缩放平移、展开动画，以及编辑真的写回卡片文件 / 图谱文件。
- `cards.py` 同步扩展：新增 chapter / node / condition / result 四种类型，
  frontmatter 白名单加上 `code / chapter / mode / color`，并新增 `summaries` /
  `set-summary` 两个子命令。
- 无头测试的 React 替身修了一个会让 hook 状态每次渲染都丢的路径 bug。
- `tests/host.mjs` 在拿不到 `zod` 的环境（比如裸克隆）里不再以模块错误崩栈，
  改为打印 `SKIP` 跳过清单相关的 D / E 两段；`package.json` 补上 `zod` 的
  peer 声明（由宿主 DSH 提供，标 optional）。只影响测试，不改运行时行为。

## v-alpha-0.2 — 分支画布 + 叙事类判定

面板角标 `v-alpha-0.2`，包版本 `0.2.0`。

### 新增：分支视图（自由画布）
- 第三个视图「分支」：无限画布 + 底部 iOS 后台式的卡片堆叠条。
- 从堆叠条把卡片**拖到画布上**（落点对准指针）或**点一下**（自动放到现有节点右边）。
- 节点之间**自由连线**：从卡片右侧的圆点拖一条贝塞尔曲线到另一张卡片。
- 同一起点的第二条连线自动命名为「选项A / 选项B …」，可随时改；线被选中时可以删掉。
- 节点可拖拽改位置；双击节点直接打开卡片正文。
- 「自动排列」按连线算最长路径分层，同层再按故事内时间上下排。
- 「清空画布」需二次确认，只动画布，不动卡片文件。
- 图例：右侧有圆点、已上画布的卡片在堆叠条上带一个小蓝点。

### 新增：在画布上设置具体时间
- 选中节点后，检查条里有「时间」（when）和「序号」（order）两个输入框。
- 时间**只写进本机图谱**，不改写卡片文件（客户端文件通道是只读的）。
- 世界线视图共用同一份时间：图谱里设的时间优先于卡片 frontmatter 的 when。
- 世界线上这类节点会标一个「画布改」。

### 新增：叙事类 / 描述类的判定
- 只有「叙事类」卡片能上世界线、进分支画布：默认是
  **场次 scene · 结构节拍 beat · 台词素材 dialogue**。
- 人物、设定、创作决定、待定问题、参考资料、项目企划、会话归档一律视为
  「关于故事的描述」，不进时间轴也不进画布。
- 判定不写死：世界线与分支视图的头部都有一排「叙事类」开关，可以逐类打开/关闭，
  开关状态按项目存在 localStorage 里。

### 修复
- 画布滚动后坐标偏移：拖动和连线原先相对**滚动容器**取坐标，滚动后会整体偏掉
  一个 scrollLeft/scrollTop。改为相对内层画布取坐标。
- 收藏 / 置顶写入时会把图谱一起覆盖掉。localStorage 现在按项目整份读写
  （star / pin / types / graph），任何一处改动都不会冲掉其它几处。
- 卡片文件被删掉后，图谱里对应的节点和悬空连线会在下次扫描时自动清掉。

### 其它
- 方片和世界线的卡片上会显示时间标签，并标出这个时间是来自卡片还是画布。
- 新增 `tests/`：无浏览器、无依赖的冒烟测试（自带一个极小的 React 替身），
  覆盖上面全部行为。`npm test` 或 `node tests/smoke.mjs`。
- 版本改为用 git 管理，tag 与面板角标同名。

## v-alpha-0.1 — 第一版常驻面板

面板角标 `v-alpha-0.1`，包版本 `0.1.0`。

- 把原来只在当前 DSH 进程里活着的动态插件，改造成 profile 常驻包：
  放进 `profiles/<profile>/node_modules/`，包名加进该 profile `package.json`
  的 `dsh.profile.bundles`，重启后依然在。
- 客户端半边手写，不走打包（`window.__ModuleLoader__.load` 约定）。
- 数据全部走现成的只读通道 `remote.workspaceFiles`，因此没有宿主半边。
- 方片视图：自适应圆角方片网格、按类型分组、标签单行横滑、星标收藏、置顶。
- 世界线视图：按 frontmatter 的 `when` / `order` 排时间轴，无时间时进入演示模式。
- 右侧栏停靠页（与对话并排）+ 会话头「剧本档案」按钮 + 左侧栏全屏页。
- 收藏 / 置顶存浏览器 localStorage，按项目根路径分组（代价：不跟项目走）。
